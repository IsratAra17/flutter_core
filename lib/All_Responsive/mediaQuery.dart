import 'package:flutter/material.dart';
import 'package:flutter_core/All_Responsive/style.dart';

class Meadia_Query extends StatelessWidget {
  const Meadia_Query({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Hello Flutter",style: HeadLine(context),),

    );
  }
}
